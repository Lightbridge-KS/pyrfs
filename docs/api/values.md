# Bytes & Perms

Typed scalars that subclass `int` — see the
[typed values guide](../guides/typed-values.md). With the `[pandas]` extra,
columns of these become the `"bytes"`/`"perms"`/`"path"` ExtensionDtypes.

::: pyrfs.Bytes
::: pyrfs.Perms
